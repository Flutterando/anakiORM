name: shelf_mssql_example
description: Example REST API using Shelf + AnakiORM + SQL Server.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: '>=3.10.0 <4.0.0'

dependencies:
  shelf: ^1.4.0
  shelf_router: ^1.1.0
  anaki_orm: ^0.1.0
  anaki_mssql: ^0.1.0
